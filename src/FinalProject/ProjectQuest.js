import React from 'react';
import './ProjectQuest.css'; // Import the CSS file for styling

const ProjectQuest = () => {
    return (
        <div className="project-quest-container">
            <h1 className="project-quest">
                How can we ensure future leaders can be supported and trained to be empowered to take on global and community issues in the for-profit and not-for-profit sectors, and create their own business opportunities?
                <div className="divider-line-container">
                    <hr className="divider-line" />
                </div>
            </h1>
        </div>
    );
};

export default ProjectQuest;
